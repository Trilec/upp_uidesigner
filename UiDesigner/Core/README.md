# UiDesigner Core

Headless persistent document and typed change-set layer.

Persistent state contains nodes, hierarchy, properties, resources and virtual size.
Selection, hover, preview rectangles, pane state and editor state remain session data.
