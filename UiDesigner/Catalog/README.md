# UiDesigner Catalog

Single source of truth for toolbox identity, runtime kind, defaults, property schema,
impact classification, code type, help and stock/native status.

The catalog contains every public runtime Ui control listed in `Ui/Ui.upp`, the six
Ui composites, layout/container primitives, three starter presets and supported stock
U++ controls.
