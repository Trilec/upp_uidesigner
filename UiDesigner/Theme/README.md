# UiDesigner Theme

Separate Theme document, preview/commit/cancel/undo/redo history and the authored
Theme Studio gallery.

The gallery reproduces the exported role buttons, title card, toggles, button family,
numeric editors, mask/password, document, tabs/tree/table/list/accordion, slider and
progress sections. It also appends a complete native Ui catalog inventory so new controls
appear automatically.
